import { GetNameForCodesPipe } from './get-name-for-codes.pipe';

describe('GetNameForCodesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNameForCodesPipe();
    expect(pipe).toBeTruthy();
  });
});
